<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RandomChatSrc.Pages.ChatRoomPage"
             Title="Chat Room">

    <Grid VerticalOptions="FillAndExpand">
        <ScrollView>
            <StackLayout x:Name="MessageContainer" VerticalOptions="Start" Spacing="10" />
        </ScrollView>
        <Grid VerticalOptions="End">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Entry x:Name="MessageEntry" Placeholder="Type your message..." VerticalOptions="Center" Margin="10"
                   Grid.Column="0" />

            <Button Text="Send" Clicked="SendMessage_Clicked" VerticalOptions="Center" Margin="10"
                    Grid.Column="1" />
        </Grid>
    </Grid>

</ContentPage>
